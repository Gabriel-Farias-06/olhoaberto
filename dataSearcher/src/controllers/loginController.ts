export default async (email: string, password: string) => {
  // LOGICA DE LOGIN
  // buscar por email
  // se não achar...
  // se achar, compara a senha
  // se diferente ...
  // se igual, retorna token e dados do usuário
  return {
    email: "email@email.com",
    accessToken: "",
  };
};
